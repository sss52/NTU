'use strict';

import TwoColumnImageText from '../two-column-image-text';

describe('TwoColumnImageText View', function() {

  beforeEach(() => {
    this.twoColumnImageText = new TwoColumnImageText();
  });

  it('Should run a few assertions', () => {
    expect(this.twoColumnImageText);
  });

});
