'use strict';

import StrategySection from '../strategy-section';

describe('StrategySection View', function() {

  beforeEach(() => {
    this.strategySection = new StrategySection();
  });

  it('Should run a few assertions', () => {
    expect(this.strategySection);
  });

});
