'use strict';

import CtaSection from '../cta-section';

describe('CtaSection View', function() {

  beforeEach(() => {
    this.ctaSection = new CtaSection();
  });

  it('Should run a few assertions', () => {
    expect(this.ctaSection);
  });

});
