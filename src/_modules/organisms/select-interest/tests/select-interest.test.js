'use strict';

import SelectInterest from '../select-interest';

describe('SelectInterest View', function() {

  beforeEach(() => {
    this.selectInterest = new SelectInterest();
  });

  it('Should run a few assertions', () => {
    expect(this.selectInterest);
  });

});
