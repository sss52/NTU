'use strict';

import InputSelect from '../input-select';

describe('InputSelect View', function() {

  beforeEach(() => {
    this.inputSelect = new InputSelect();
  });

  it('Should run a few assertions', () => {
    expect(this.inputSelect);
  });

});
