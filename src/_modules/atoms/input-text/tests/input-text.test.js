'use strict';

import InputText from '../input-text';

describe('InputText View', function() {

  beforeEach(() => {
    this.inputText = new InputText();
  });

  it('Should run a few assertions', () => {
    expect(this.inputText);
  });

});
